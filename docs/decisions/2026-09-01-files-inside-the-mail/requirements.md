# R1..R9 — what was actually asked, in whose words, on what day

## Said in this run

- **R1** — Kate Yanchenko, 2026-09-01, the sentence that started this run:
  "Make sure that after inside mcp_Lamoom will be results provided we will send an email to a
  user with including files inside the email, by referencing them relative path with {{}}, with
  inline:path, or simply {{path}} to attach, or link {{link:path}}"
- **R2** — Kate Yanchenko, 2026-09-01, the same sentence, second line:
  "To allow user to review online results"

Read together: three forms, one grammar, written into the body of the mail.
`{{inline:path}}` shows in the message. `{{path}}` hangs off it. `{{link:path}}` is a link she
taps. "Relative path" is the path she already sees everywhere else — from her root, no id.

## Said before, and still standing

- **R3** — Kate, 2026-09-01, on this role: "Update a rule inside /mcp_lamoom when writing a
  diagram - all files should be saved inside Lamoom, so call mcp.lamoom.com to save files"
- **R4** — Kate, 2026-09-01, on this role: "if we don't understand smth - we don't add it" -
  please add as a rule
- **R5** — Kate, 2026-09-01, on this role: "we need to name things by when we should call a
  loop, nto something vague"
- **R6** — Kate, 2026-09-01, on this role: "Better to not write, that write not in well
  explainable and thoughful way"
- **R7** — Kate, 2026-09-01, on this role: "and delete anything , and copy loops exactly what we
  understand, do not rewrite their steps"
- **R8** — Kate, standing preference on every prompt instruction: "make it clear. Explain like
  for a rubber duck. Be concise."
- **R9** — Kate, standing preference on every prompt instruction: "do not ban actions, instead
  just delete anything what makes it to think that way! And narrow it to think a desired way."

R8 and R9 decide the shape of the SKILL.md edit before a word of it is written: the delivery
line is narrowed until writing `{{...}}` is the obvious move, and the wording that made the
old way look right is deleted in the same edit rather than a prohibition being stacked on top
of it.
